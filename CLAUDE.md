cat > CLAUDE.md << 'EOF'
# FinishLine

アニメの完走予測サービス。

仕様は `docs/requirements.md` と `docs/ui_spec.md` を参照。
作業前に必ず該当セクションを読むこと。

## 絶対に守ること

### データリーク防止
- データ分割は必ず user_id 単位。作品単位で分割しない
- 作品の平均完走率を特徴量に使う場合、訓練ユーザーのみから算出する
- rating=0 は「未評価」。欠損として扱う（最低評価ではない）

### 評価
- AUC / PR-AUC を使う。Accuracy は使わない
- 履歴フル使用の精度を実サービスの精度として報告しない

### UI表現
- 「切る」「切った」「脱落」「ドロップ」を UI に出さない
- 確率は「完走率○%」で統一。離脱確率を表示しない
- 断定表現を使わない
- 5本未満で結果を出さない。20本を超えて追加を促さない
- MAL の作品画像を直リンクしない

## 進め方
Phase 0 の Go 判定が出るまで Phase 1 以降に着手しない。
1フェーズずつ実行し、完了したら停止して報告すること。
EOF